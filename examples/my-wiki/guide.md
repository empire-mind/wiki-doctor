# Guide

A well-behaved page. Linked from [[index]], links back to [[index]].

```python
# [[not-a-real-link]] inside a fence is ignored by the linter
```

And `[[also-ignored]]` in inline code is ignored too.
